import React from 'react';
import styles from './uploadResume.less';
import { Button, Popconfirm, Table } from 'antd';

export default function Page() {
  return (
    <div>
      <h1 className={styles.title}>Page upload</h1>
    </div>
  );
}
